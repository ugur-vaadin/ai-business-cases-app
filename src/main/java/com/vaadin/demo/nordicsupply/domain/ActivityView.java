package com.vaadin.demo.nordicsupply.domain;

/** The screens the activity log distinguishes; the name is what the {@code view_name} column holds. */
public enum ActivityView {
    INSIGHTS,
    CLAIM_FORM,
    CATALOGUE
}
